1. What is Git?

Git is a Version Control System (VCS).
It is a tool that helps you track changes in your code.
With Git, you can simply go back to an earlier version.

Simple definition-
Git is a distributed version control system used to track, manage, and collaborate on changes in source code

2. What is GitHub?
GitHub is a cloud-based platform where Git repositories can be stored and shared.

1. Local Version Control System-
Everything is stored on your own computer.

2. Centralized Version Control System (CVCS)
There is one central server.
       Server
      /   |   \
     /    |    \
   Dev1  Dev2  Dev3

Examples:
SVN
CVS
Perforce
Developers connect to the central server.

3. Distributed Version Control System (DVCS)
Every developer has a complete copy of the repository and its history.
             Remote Repository
                   |
        -----------------------
        |          |          |
      Dev A      Dev B      Dev C
Git is a Distributed Version Control System.

This is one of the most important points to remember for interviews.

6. Git Repository Types
You will commonly hear about two Git repository locations:
Local Repository
*) Repository on your computer.
Laptop
 └── MyProject
      └── .git

*) Remote Repository
Repository stored on a remote service/server.
Examples:
GitHub
GitLab
Bitbucket

Git work Flow-
Working Directory
       ↓
Staging Area
       ↓
Local Repository
       ↓
Remote Repository


1) Working Directory
Your actual project files.
index.html
app.js
style.css
2) Staging Area
Files selected for the next commit.
git add .
3) Local Repository
Where Git stores your committed history.
git commit -m "Added login feature"
4) Remote Repository
Usually GitHub.
git push

Different Types Of Commands used In github are
1) git init-initializing a git in project folder
2) git status-git status tells you what has changed,
 what is staged, and what Git is currently tracking.
3) git add - git add is used to move your changes from the
 Working Directory to the Staging Area.
4) git commit -Git opens a text editor asking you to enter a commit message.
5) git commit -m "commit message"- commit is commit the message -m message of the commit and "" inside write 
description of the message----Creates commit directly with the given message

6) git remote add origin url of repo-Adding exist remote Add a remote repository URL to my local Git repository and give that remote the name origin.
origin is the name of your remote repository.
main is the branch you want to push.
Part	Meaning
git	Use Git
remote	Work with remote repositories
add	Add a new remote
origin	Name given to the remote
URL	Address of the GitHub repository
7) git push -Send your local commits to a remote repository such as GitHub.
8) git puh -u origin main - origin is the name of your remote repository.
origin is the name of your remote repository.
-u means set the upstream/tracking relationship.